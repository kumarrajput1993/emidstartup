The build in the directory needs node

1. Install node from nodejs.org
2. Install few stuff with (in the app directory)
    npm install gulp -g
    npm install bower -g
    npm install gulp
    npm install karma-cli -g //for running unit test cases

    npm update
    bower install

3. do gulp dist to compile app directory



